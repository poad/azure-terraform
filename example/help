========================================================================================================

  Your CDKTF TypeScript project is ready!

  cat help                Print this message

  Compile:
    pnpm get           Import/update Terraform providers and modules (you should check-in this directory)
    pnpm compile       Compile typescript code to javascript (or "pnpm watch")
    pnpm watch         Watch for changes and compile typescript in the background
    pnpm build         Compile typescript

  Synthesize:
    cdktf synth [stack]   Synthesize Terraform resources from stacks to cdktf.out/ (ready for 'terraform apply')

  Diff:
    cdktf diff [stack]    Perform a diff (terraform plan) for the given stack

  Deploy:
    cdktf deploy [stack]  Deploy the given stack

  Destroy:
    cdktf destroy [stack] Destroy the stack

  Test:
    pnpm test        Runs unit tests (edit __tests__/main-test.ts to add your own tests)
    pnpm test:watch  Watches the tests and reruns them on change

  Upgrades:
    pnpm upgrade        Upgrade cdktf modules to latest version
    pnpm upgrade:next   Upgrade cdktf modules to latest "@next" version (last commit)

 Use Providers:

  You can add prebuilt providers (if available) or locally generated ones using the add command:
  
  cdktf provider add "aws@~>3.0" null kreuzwerker/docker

  You can find all prebuilt providers on pnpm: https://www.pnpmjs.com/search?q=keywords:cdktf
  You can also install these providers directly through pnpm:

  pnpm install @cdktf/provider-aws
  pnpm install @cdktf/provider-google
  pnpm install @cdktf/provider-azurerm
  pnpm install @cdktf/provider-docker
  pnpm install @cdktf/provider-github
  pnpm install @cdktf/provider-null

  You can also build any module or provider locally. Learn more https://cdk.tf/modules-and-providers

========================================================================================================
